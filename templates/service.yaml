apiVersion: v1
kind: Service
metadata:
  name: {{ include "rancher-ui.fullname" . }}
  labels:
    {{- include "rancher-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rancher-ui.selectorLabels" . | nindent 4 }}
