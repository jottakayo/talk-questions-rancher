apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rancher-ui.fullname" . }}-test-connection"
  labels:
    {{- include "rancher-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rancher-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
